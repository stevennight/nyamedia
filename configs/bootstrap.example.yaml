server:
  host: 0.0.0.0
  port: 7001
  public_base_url: http://127.0.0.1:7001

storage:
  data_dir: ../data
  database_url: postgres://nyamedia:nyamedia@127.0.0.1:5432/nyamedia?sslmode=disable
  strm_output_dir: ../data/strm

auth:
  bootstrap_username: admin
  bootstrap_password: admin

webhook:
  # Set a strong random token before exposing webhook endpoints.
  token: ""

logging:
  level: info
